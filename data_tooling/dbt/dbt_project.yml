# dbt project skeleton for the Lakehouse proof-of-concept.
# Complete this file when the Bronze/Silver/Gold model boundaries are finalized.

name: bmo_lakehouse_dbt
version: "1.0.0"
config-version: 2
profile: bmo_lakehouse
model-paths:
  - models

# TODO: define model configs by layer.
# models:
#   bmo_lakehouse_dbt:
#     bronze:
#       +materialized: table
#     silver:
#       +materialized: view
#     gold:
#       +materialized: table
