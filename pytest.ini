[pytest]
pythonpath = .
testpaths = src/tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: tests que requieren integracion real con bases de datos o servicios
